# Docker Environment Configuration for Blue Escape Backend
# This file contains environment variables specifically for Docker Compose setup

# Application Environment
NODE_ENV=development
PORT=3000

# Database Configuration for Docker
# Note: DB_HOST is set to the MySQL service name in docker-compose.yml
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=blue_escape_user
DB_PASSWORD=blue_escape_password
DB_DATABASE=blue_escape_db

# Application Configuration
JWT_SECRET=your_jwt_secret_for_docker_environment

# MySQL Root Configuration (for docker-compose)
MYSQL_ROOT_PASSWORD=root_password
MYSQL_DATABASE=blue_escape_db
MYSQL_USER=blue_escape_user
MYSQL_PASSWORD=blue_escape_password

# phpMyAdmin Configuration
PMA_HOST=mysql
PMA_PORT=3306
PMA_USER=blue_escape_user
PMA_PASSWORD=blue_escape_password